# =================================
# DREAM ANALYZER - TEST ENVIRONMENT VARIABLES
# =================================
# Тестовые переменные для Jest тестов

# Telegram Bot Configuration (Test)
TEST_BOT_TOKEN=test_bot_token

# Supabase Configuration (Test)
TEST_SUPABASE_URL=test_db_url
TEST_SUPABASE_SERVICE_KEY=test_service_key

# AI Service Configuration (Test)
TEST_GEMINI_API_KEY=test_gemini_key

# Authentication Secrets (Test)
TEST_JWT_SECRET=test_jwt_secret
TEST_REFRESH_SECRET=test_refresh_token_secret

# Frontend URLs (Test)
TEST_TMA_URL=http://localhost:5173
TEST_ALLOWED_TMA_ORIGIN=localhost:5173
TEST_ALLOWED_WEB_ORIGIN=localhost:3000

# API Configuration (Test)
TEST_VITE_API_BASE_URL=http://localhost:8080
TEST_VITE_WEB_LOGIN_API_URL=http://localhost:8080/web-login

# =================================
# ДОПОЛНИТЕЛЬНЫЕ ТЕСТОВЫЕ ПЕРЕМЕННЫЕ:
# =================================
# Переменные для изоляции тестов
NODE_ENV=test
JEST_TIMEOUT=30000

# =================================
# ПРИМЕЧАНИЯ:
# =================================
# 1. Эти переменные используются только для тестов
# 2. Реальные значения API ключей не нужны для unit тестов
# 3. Для интеграционных тестов могут потребоваться реальные тестовые базы данных
# 4. Файл автоматически загружается Jest через jest.config.js
